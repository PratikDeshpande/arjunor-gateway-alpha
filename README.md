# arjunor-gateway-alpha

This project is used to prototype the API Gateway for Arjunor DB

The purpose of the gateway is to:

1. Validate a user's API keys

2. Estimate the storage and or compute cost of the request (with relation to rate limiting)

3. Validate the request does not exceed user's compute and storage limits

